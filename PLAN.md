# 戰國麻將列傳 — 四人線上版　開發計畫

> 建立於 2026-09-15。從現有的單機兩人版升級為四人連線對戰。

## 專案定位

| | 路徑 |
|---|---|
| 現有單機版（來源） | `~/Documents/DILA/麻將戰國列傳/戰國麻將列傳.html` |
| 現有版部署 clone | `~/Documents/DILA/jcmj-git/` → https://allenkwl.github.io/jcmj/ |
| 連線架構參考 | `~/Documents/DILA/小球貓電鐵/game-web/src/net-v1.22.js` |
| 本專案 | `~/Documents/DILA/戰國麻將列傳-四人線上版/` |

---

## 一、設計決策（已定案）

| 項目 | 決定 |
|---|---|
| 人數 | 四人，人數不足由 AI 補位 |
| 對戰單位 | **一場四局**（東風戰），一場約 15–20 分鐘 |
| 勝負與土地 | 一場結束，**最後一名失去一國**，第一名獲得一國 |
| 征服目標 | **開局時自己選**要征服哪一國，與對手是誰無關 |
| 對手顯示 | 對戰畫面顯示對手身分（玩家名稱／頭像），但不影響征服判定 |
| 進度保存 | 個人持久化，存本機 `localStorage` |
| 身分 | **Firebase 匿名登入**（玩家無感，沒有登入畫面），用途是讓權限規則能藏手牌 |
| Game over | 土地全失 → 重選國家再戰（換國家＝換技能，重來是新體驗） |
| 成長方式 | **只強化技能，不動牌山**（特殊牌型機率維持原樣） |
| 技能成長 | 主動技能使用次數 1→2→3；被動技能倍率 1.3→1.4→1.5，依 `conquered.length` |
| AI | **不作弊**，改讀牌河推測；一桌一個難度 |
| AI 難度設定 | **建房時決定**，寫進 `groups/{名}` 節點，大廳列表顯示 |
| 大廳分級 | **不分級、不擋人**，只顯示 AI 難度與房內玩家征服數 |
| 畫面方向 | **強制橫式**（已有旋轉引導頁），直式放不下四人版面 |

### 為什麼特殊牌型機率不動
四家共用一副牌山，給某家好起手等於從其他三家拿走好牌。改成只強化技能，公平性爭議歸零。
（現有的偏置發牌機制 `makeBiasedDeck_internal` 保留原樣，70% 機率仍會組牌，只是不隨征服數變動。）

### 為什麼要匿名登入
Firebase 權限規則沒有 `auth.uid` 就無法分辨「誰在讀」，也就寫不出「這座位的手牌只有本人能讀」。
匿名登入對玩家完全無感（一次 `signInAnonymously()`，無 UI），持久度與 localStorage 相同，
但換來手牌真的藏得住。不做的話，打開 F12 就能看到全場的牌。

---

## 二、不用改的東西（保值資產）

這決定了工程規模的上限 —— 以下約 3,000 行 + 12MB 圖完全沿用：

- 美術、音效 SFX、程序化 BGM（近 1,000 行，8 場景 + 7 國專屬曲）
- 七國資料 `KINGDOMS`、技能框架 `KINGDOM_SKILLS`、戰書文案 `WAR_DECL`（約 250 行）
- 牌面 Canvas 繪製 `drawFace` / `drawWan` / `drawTong` / `drawTiao` / `drawHonor`（**除了要新增旋轉支援**）
- 特殊牌型定義 `SPECIAL_HANDS`（10 種，資料驅動、玩家無關）
- 規則核心純函式（見下）

真正要動的是遊戲流程層，約 3,000 行裡的六到八成。

---

## 三、工作階段

### 階段一：四人化（單機先做完）

> 這是地基，同時是線上化的八成前置工作。
> **反過來先做線上，規則核心會被寫兩次。**

**1. 抽出純規則模組**　✅ 已完成 2026-09-15
以下已是乾淨的純函式（不碰 `G`），約 400–600 行，抽成獨立檔案：
`canWinInHand` `canWinOnDiscard` `canFPair` `canFNS` `canWin14` `findChi` `findAllChi`
`shantenNum`（含 `_shantenDFS` / `shantenStd` / `shantenQidui` / `shantenGuoshi`）
`isTenpaiHand` `checkSpecialHand` `_meldStats` `calcScore`

同時**補上振聽規則** —— 現在完全沒有實作。補了之後：
- AI 的正當防守才有立足點（現物 100% 安全）
- 補掉玩家端漏洞（現在可以打掉自己的胡牌再等別人打出來榮和）

> **產出**：`src/rules-core.js`（468 行，25 個匯出，零全域依賴）
> 由 `tools/build-rules-core.py` 從舊版自動抽取，可重跑；對照表見 `docs/extraction-map.md`。
> `test/rules-core.test.js` 37 項測試全過。
> 順手找到原版一個 bug（六對子時聽牌提示少報），詳見 extraction-map.md。

**2. 狀態重構 `G.pH/oH` → `G.seats[4]`**　【最苦的一步】　✅ 模型完成 2026-09-15
- `pH/oH`、`pM/oM`、`pD/oD` → `seats[i].{hand, melds, discards}`
- `G.pTurn`（布林）→ `G.turn`（座位索引）+ `G.dealer`（莊家）
- `G.pile`（`shift()` 取牌）→ `wall` + `drawIdx` + 王牌邊界（可事後驗證發牌）
- 實測影響 **58 個函式、311 處欄位存取**（原估 45／250，偏低約三成）

> **產出**：`src/game-state.js`（341 行，23 個匯出，純 JSON 狀態）
> 對照表見 `docs/state-model.md`；`test/game-state.test.js` 92 項測試全過。
> 順手修掉兩個舊版問題：嶺上牌可以連開 136 個槓（流局後照樣槓）、
> 被鳴走的牌不再算振聽。兩項都有迴歸測試。
>
> ⚠️ **這一步只做完狀態模型本身。** 那 58 個舊函式是在第 4 步（流程狀態機）、
> 第 5 步（AI）、第 6 步（四方版面）接過來的，不是在這裡改欄位名 ——
> 因為其中一半（`render*`／`upd*`／`_auto*`）本來就要為四方版面重寫。
> 理由與取捨寫在 `docs/state-model.md` 開頭。

**3. 宣告仲裁子系統**　【全新，300–500 行】　✅ 已完成 2026-09-15
現況等於零 —— 兩人局天然無衝突，`chkInt` 與 `oppDecide` 是兩條各自獨立的分岔。
需要新增：
- 收集三家意向
- 優先權裁決：胡 > 槓/碰 > 吃，且**吃僅限下家**
- 多家同時胡的處理策略
- 逾時自動 pass
- 狀態層已經備好的接口：`hand.lastDiscard.seq`（對齊宣告視窗）、
  `hand.lastKongAdded`（搶槓）、`R.canChiSeat()`（吃僅限下家）

> **產出**：`src/claim-arbiter.js`（404 行，16 個匯出，視窗物件為純 JSON）
> `test/claim-arbiter.test.js` 98 項測試全過。
> 裁決順序：胡 > 槓/碰 > 吃。多家同時胡三種策略可選（`head` 頭跳／`multi` 多家和／
> `abort3` 三家和流局），**預設頭跳** —— 一局只有一個贏家，計分與一場四局的排名都不用改。
> 順手接上魏國鐵騎（擋吃碰槓、不擋榮和）與搶槓視窗。
>
> 實作後修正的兩個認知：
> - **碰與槓之間不需要排序** —— 一種牌只有 4 張，場上打出 1 張剩 3 張，
>   要兩家都能碰得湊 2+2=4 張，合法牌況下湊不出來。程式裡的排序是防呆不是規則。
> - 「無牌可宣告者不跳窗」落在 `openWindow()` 回 `null`，是唯一一道閘。
- ⚠️ **無牌可宣告者不跳窗** —— 否則一局 60 次打牌每次都等滿逾時，會多出 5 分鐘空轉

**4. 流程狀態機**　✅ 已完成 2026-09-15
取代現在 **59 個 `setTimeout`** 串起來的呼叫鏈
（`pDraw` → `doDiscard` → `oppDecide` → `oppTurn` → `chkInt` → `passInt`）。
**這是能不能接上網路的分水嶺。**

> **產出**：`src/flow.js`（517 行，10 個匯出，零計時器）
> `test/flow.test.js` 94 項測試全過，其中主力是「60 場全程不變量」——
> 每一個停頓點都驗牌數守恆、手牌張數、不卡住。
>
> 為什麼這一步解得開網路：setTimeout 鏈把「接下來該發生什麼」藏在**呼叫堆疊**裡，
> 堆疊不能序列化、不能傳給別台、斷線重連接不回來。改成狀態機之後，
> 下一步完全由 `hand.phase` + `hand.turn` + `hand.claim` 三個 JSON 欄位決定。
>
> 四個硬性約束（有測試釘死）：
> 1. **模組裡沒有任何計時器** —— 演出節奏是畫面層的事
> 2. 外部輸入只有一道門 `submit()`，人類／AI／網路走同一套合法性檢查
> 3. `run()` 一路推進到「需要外部輸入」或「這局結束」為止（線上由 driver 跑）
> 4. AI 不住在這裡，`ctx.chooseDiscard` 是第 5 步的掛載點
>
> events（`draw`/`discard`/`claimOpen`/`meld`/`win`/`handEnd`…）是畫面層與網路層的契約，
> 形狀對齊電鐵的 cmds 演出指令。
>
> ⚠️ **計分是暫行版**：只算贏家自己的分 + 放銃者扣同額，讓一場四局跑得完。
> 四家之間真正的點數移轉（誰付給誰、莊家加成、流局聽牌罰符）是第 7 項。

**5. AI 改造**　✅ 已完成 2026-09-15
- 掛載點已經備好：`flow.js` 的 `ctx.chooseDiscard(hand, seat)`。
  目前掛的是 `defaultChooseDiscard`（向聽數貪心，同分時偏好打字牌與么九），
  **只有進攻、零防守、不看別家手牌** —— 第 5 步要補的是防守那一半。
- 宣告的部分（該不該吃碰槓）已經在 `claim-arbiter.js` 的 `decideClaim()`，
  行為與舊版一致（初學不鳴／普通看向聽改善／高手積極），第 5 步一併重寫。
- 參數化：`aiD(hand)` 現在直接讀全域 `G.oM` `G.pH` `G.pM`，三家 AI 會共用狀態，必須改成 `aiD(hand, melds, table)`
- 拿掉作弊：`getPlayerWaitingTiles()` 現在直接 `canRon(G.pH, …)` 偷看玩家手牌
- 改成牌河推測。難度改用**資訊完整度**分級：

  | 難度 | 防守依據 |
  |---|---|
  | 初學 | 不防守 |
  | 普通 | 只看現物 |
  | 高手 | 現物 + 筋 + 副露推測（威脅度） |

> **產出**：`src/ai.js`（347 行，20 個匯出）
> `test/ai.test.js` 66 項測試全過。接線：`AI.flowHooks()` 一次接好 flow 的兩個掛載點。
>
> **三個病都修了，而且每一個都有「會抓到舊行為」的測試**（實際植入舊程式驗證過）：
>
> | 病 | 舊版 | 現在 | 測試怎麼抓 |
> |---|---|---|---|
> | 作弊 | `getPlayerWaitingTiles()` 直接翻玩家手牌 | 只吃公開資訊 | 100 個盤面，把別家手牌換成聽牌，決定必須完全不變 —— 植入作弊後 11 個盤面變號 |
> | 三家共用狀態 | `aiD` 讀全域 `G.oM` | 全部參數化 | 兩家不同副露數各自算 |
> | 全程龜縮 | danger 8000 對一向聽 1000 | `DANGER_MAX=150`，且聽牌時壓力係數降到 0.2 | 構造「安全牌要付一個向聽」的局面：新版打生張進聽牌，舊版權重打安全牌停在一向聽 |
>
> 統計佐證：四家全高手時，新版 16 局胡 14 局；還原舊版權重後 12 局只胡 4 局（流局 8 局）。
>
> **兩個偏離原規劃的地方**：
> 1. 初學**不是**隨機棄牌。PLAN 原文的「維持現在的隨機棄牌」是在描述兩人版現狀，
>    但四人局有三家 AI，全隨機會讓整桌沒人做得出牌、一路流局。
>    改成「向聽數貪心但在前兩級選項裡隨手挑」—— 會失誤、但形得成牌。
> 2. 進攻多了**受入**（打掉這張之後還有幾張進張），同向聽時據此排序。
>    成本控制見 `UKEIRE_SHANTEN_GATE` / `UKEIRE_MAX_CANDIDATES`。
>
> ⚠️ **不准用 `Math.random`** —— 線上版要能重放，AI 一擲骰子 driver 與旁觀者就會分岔。
> 初學的「隨手打」用牌河長度推偽隨機。有測試釘死（原始碼掃描 + 整場事件序列比對）。
>
> **效能**：一次棄牌決策約 8ms（向聽數有快取，熱快取 0.2ms），一場四局約 4.6 秒。
> 實際遊戲綽綽有餘（人類本來就要想幾秒），但測試不能大量跑 —— 大迴圈一律注入便宜的棄牌策略。

- 註：作弊只用在防守（`aiD` 的 `danger` 項），**進攻邏輯完全乾淨**，拿掉不影響做牌速度
- 註：現在 expert 的 `dangerWeight=80` × `danger=100` = 8000，遠大於一向聽的 1000，
  等於玩家一聽牌 AI 就完全龜縮。四人局有三家輪流聽牌，會全程不做牌 —— **本來就必須改**

**6. 四方版面**　✅ 已完成 2026-09-16
- CSS grid 重設（現在 `.mja` 是上下兩方 `"oz oav" / "pb pb" / "pz pav"`）
- 側面座位的牌需要**旋轉繪製** —— Canvas 目前沒有這能力
- 手機橫式版面重做、`applyScale()` 尺寸計算重算
- 估：CSS 200–300 行、新 DOM 60 行、render 層重寫約 150 行

> **產出**：`mj4.html`（版面骨架 + 控制層）、`src/tiles.js`（牌面繪製含旋轉）、
> `src/render.js`（狀態 → 四方版面）。用 `http://localhost:9987/mj4.html` 預覽
> （`~/.claude/launch.json` 的 `mj4`，跑 `tools/dev-server.py`）。
>
> ⚠️ **開發伺服器一定要用 `tools/dev-server.py`，不要用 `python3 -m http.server`。**
> 後者不送任何快取標頭，瀏覽器會用啟發式快取 —— 改完 `src/*.js` 重新整理，
> 載進去的還是舊檔。這個坑很難察覺：HTML 有更新、畫面看起來變了，但模組是舊的
> （實測就是這樣，排序改了半天畫面完全沒反應）。
>
> **已完成**
> - 四方版面：三欄（左家 95px／中央／右家 95px），中央再分對家、牌河區、自己
> - 牌面旋轉：canvas 長寬對調 + translate/rotate，繪製程式不用知道自己被轉幾度
> - 四家牌河各照**自己的視角**排（一列從他的左到他的右，列往桌子中央推進）
> - 尺寸自適應 `fitSizes()`（＝PLAN 說的 `applyScale()` 四人版）：
>   手牌受寬度與高度雙重限制，牌河照**實際用到的列數**算 ——
>   開局一列時 23×32，打到三列才縮到 12×15
> - 旋轉引導頁（直式擋掉）
>
> - 手牌自動排序（萬→筒→條→字，與舊版 `sortH` 同序）。
>   **剛摸進來的那張留在最右邊、拉開一個間隔**，打掉或用掉之後才併回牌組 ——
>   麻將的摸切慣例，玩家要一眼看到自己摸到什麼。排序在狀態層做，
>   所以畫面上的索引就是 `flow.submit({tileIndex})` 要的索引，不用換算。
> - 互動**照原版戰國麻將列傳的操作模型**，沒有常駐按鈕列：
>
>   | 動作 | 操作 | 原版對應 |
>   |---|---|---|
>   | 打牌 | 點一下選取，再點同一張打出 | `renderPH` 的 `if(G.sel===i) doDiscard()` |
>   | 打牌（數字鍵） | `` ` 1 2 3 4 5 6 7 8 9 0 - = ⌫ `` **按一下直接打出** | `_quickMap`（L7220） |
>   | 移動選取 | ← → | L7235 |
>   | 確認打出 | 空白／Enter | L7242 |
>   | 吃碰槓胡 | 右下角浮動視窗，含被宣告的那張牌 | `#int-pop`（L178） |
>   | 自摸／暗槓／加槓 | 只在做得到時浮在手牌上方 | 原版是操作列的按鈕，改成浮動 |
>   | 一局／一場結算 | 置中浮動視窗 | |
>
>   拿掉下方按鈕列不只是為了像原版 —— 那條列吃掉 34px 縱向預算，
>   而 iPhone SE 橫式只有 375px，省下來全是牌河的。
>
> - **全螢幕**（照原版 L1573）：第一次的點擊或按鍵進全螢幕，只做一次
>   （Fullscreen API 規定要有 user gesture，不能一載入就要）。
>   iOS 跳過 —— 它不支援 Fullscreen API，Safari 加到主畫面本來就是全螢幕。
>   `fullscreenchange` 會重算尺寸（進出全螢幕的可用高度不同）。
>
>   ⚠️ **不要把任何功能綁在 Esc 上。** Esc 是瀏覽器保留給「離開全螢幕」的鍵，
>   `preventDefault()` 擋不住。我一度把「取消選取」綁上去，結果按了會跳出全螢幕，
>   看起來像遊戲自己把全螢幕關掉。要換一張牌就直接點／按另一張，原版也是這樣。
>
> - **副露擺在手牌旁邊，不另外開一列。** 每鳴一組手牌就少三張，
>   所以「手牌＋副露」的總寬是 `w×(14 − 1.05m)` —— 副露越多反而越窄。
>   橫向零成本，卻省下自己 36px ＋ 對家 28px 兩列。
>   實測 667×375：11 張手牌＋3 張副露同一列，共 479px。
>
> - **吃碰槓的按鈕裡直接畫出會組成什麼**（仿原版 `showIPop`），不是寫「吃 345」。
>   吃有多種組合時，讀文字要換算，看牌不用。
>
> - **宣告視窗錨在自己手牌正上方**。原版是 `bottom:80px` —— 那個 80px 是為了
>   讓開它底下的按鈕列；我們沒有按鈕列，擺右下角會壓到手牌右半邊（實測 667×375）。
>
> - **AI 的節奏**：`flow.run()` 預設一口氣跑到需要玩家輸入為止，
>   三家 AI 會在同一個 tick 全部打完、三張牌同時冒出來。
>   加了 `ctx.pauseAfter`（一組 event 型別，吐出其中任何一種就先回去），
>   由控制層決定隔多久再叫下一次：打牌 620ms、鳴牌 820ms、全過 260ms。
>   實測一圈四家約 3.6 秒 —— 動作很快的人的速度。
>
>   ⚠️ `pauseAfter` **不是計時器**，`flow.js` 裡仍然一個 `setTimeout` 都沒有。
>   它只提供「停在哪裡」，「停多久」是畫面層的事（第 4 步的硬性約束沒有破）。
>
>   ⚠️ 踩過的坑：`waiting` 回 **null** 代表「流程可以自己往前走」，不是「沒事做」。
>   加了 pauseAfter 之後這個狀態變得很常見（AI 打完牌時往往正好停在
>   「該輪下一家摸牌」），控制層把 null 當成沒事做的話，AI 打完第一張就永遠不動。
>
> - **美術已移植**：`tools/extract-assets.py` 把舊版的 base64 抽成 `assets/` 底下
>   65 個獨立檔（8.56 MB），`src/assets.js` 負責載入。
>   牌面 34 張開局前載完，背景用到才載。
> - **音效與 BGM 已移植**：`tools/build-audio.py` 從舊版抽出 SFX（184 行）與
>   BGM（813 行）產生 `src/audio.js`（1039 行）。**不要手改產出檔**，改產生器。
>   音效掛在 flow 吐出的 events 上，不是各動作裡各自呼叫 ——
>   線上版重放別台的動作才會有聲音。
> - **全螢幕按鈕只在非 iOS 的手機顯示**（照原版 `_updateFSBtn`）。
>   桌機不需要按鈕：第一次點擊或按任意鍵就進全螢幕。
> - **iPad 直式可用**。原本一律擋直式，門檻改成 `max-width:600px` 只擋手機 ——
>   PLAN 的「直式放不下」是照 iPhone 14 的 393px 算的，
>   但 iPad 直式有 768～834px，比 iPhone SE 橫式（667px）還寬。
>   實測 768×1024：牌桌 756×760、0 重疊。
>   牌桌另加 `max-height:min(100%,760px)` 維持方形並置中，
>   否則直式會把中央拉成一條長條、中間空一大片。
>
> - **主公頭像**：名牌旁邊的小頭貼，加上**輪到誰就在牌桌中央秀那一國的主公**。
>   中央那張擺在最底層、邊緣用圓形遮罩化掉（原圖是白底人像，直接放會是白方塊），
>   被牌擋到一部分是刻意的 —— 它是氣氛不是資訊。
>   `tools/build-avatars.py` 從舊版的 `KING_AVATARS`（1024²）與
>   `KING_AVATARS_ACTION`（300²）壓成 96px／384px 的 WebP：
>   七國合計 **251 KB**（原圖約 2 MB）。名牌只畫 22px，
>   直接用原圖等於為了 22px 下載 200 KB。
> - **牌桌背景換成本國的戰國戰爭場景（黑化）**。
>   `tools/build-backgrounds.py` 把 `KINGDOM_BGS` 的 Q 版戰場圖壓成
>   1280px WebP：亮度降到三成、降飽和、壓一層墨綠（接回氈面色 `--felt`）、加暗角。
>   七國合計 **306 KB**（原圖約 3.5 MB）。上面再疊半透明漸層。
>
>   為什麼要壓這麼暗：麻將的牌面是高對比的白底黑字紅字，
>   背景一亮就讀不動；紅色旌旗還會跟紅中撞色、綠旗跟發撞色，所以要降飽和。
>
>   中央主公像背後另外墊一圈暗暈 —— 背景中央也有人物，不墊的話主公會糊進去。
>   比把整張背景再壓暗好，那樣會連四周的戰場細節一起犧牲。
>
> - **七國資料已移植**：`tools/build-kingdoms.py` → `src/kingdoms.js`。
>   只抽 `KINGDOMS`（id／名稱／君主／顏色／戰力）——
>   **技能故意不抽**，四人版的係數在 `scoring.js`，
>   舊版的 `KINGDOM_SKILLS` 抽過來會變成兩份互相矛盾的真相。
>
> - **標題畫面與選國畫面**（2026-09-16）。三張全螢幕覆蓋層：標題 → 選國 → 牌桌，
>   各自配對應的 BGM 場景（`title` / `select` / `game`）。
>   背景用舊版的原圖，只縮尺寸與輕暗角（不像牌桌背景那樣壓暗 —— 這兩張圖本身是主角），
>   各約 190 KB。
>
>   選國畫面是三步：
>
>   | | |
>   |---|---|
>   | ① 本國 | 七國，決定技能。卡片帶主公頭像、國名、描述、技能名 |
>   | ② 征服目標 | 剩下六國（排除本國）—— **這是階段二第 8 項的需求** |
>   | ③ 對手強度 | 初學／普通／高手，套用到三家 AI |
>
>   把②放這裡是因為選國畫面本來就是它的載體，分開做會把同一頁改兩次。
>   ⚠️ **選了之後只存進 `match.conquestTarget` 並顯示在 HUD ——
>   一場四局結束後的土地增減還沒做**（階段二第 10、11 項）。
>
>   **對手自動抽籤並逐一揭曉**（2026-09-16）。讓玩家選對手沒有意義 ——
>   PLAN 的設計決策寫明「征服目標開局自己選，與對手是誰無關」——
>   但「跟誰打」是開局最想知道的事，所以要看得到抽出了誰：
>   選完征服目標後，三張對手卡以 260ms 間隔逐一彈出，橘框加名牌。
>
>   抽籤用 seed 推而不是 `Math.random`，而且**揭曉時抽好就存起來**，
>   `newMatch()` 直接用那一批 —— 在那裡重抽的話，畫面上看到的跟實際對手會不一樣。
>
>   牌桌背景與中央主公跟著玩家的本國走。
>
> **還沒做**
> - `WAR_DECL`（戰書文案）還在舊檔裡
> - 英雄榜／存檔（舊版有 `showLeaderboard`、`saveProgress`）
> - 標題畫面、選國畫面（背景圖已經抽出來了，只差接上）
> - 征服系統的 UI（階段二）
> - 牌面圖片：萬／筒／條／字**目前是程序繪製的**。
>   舊版那 34 張牌面是 12 MB base64 圖（`drawWan` 等走的是 `ctx.drawImage`），
>   PLAN 把「牌面 Canvas 繪製」列在保值資產裡，實際讀下去只對了一半。
>   `tiles.js` 留了 `setFaceImages()` 插槽，階段四拆圖之後塞進去就會自動改走圖片。
>
> ### ⚠️ 版面預算要修正：中央動畫區沒被算進去
>
> PLAN 原本的縱向預算是
> HUD 24 + 對家 114 + 自己 150 + 按鈕 40 + 間隙 40 = **368px**，
> 對 iPhone 14 橫式的 393px 剛好剩 25px。
>
> 但那份預算**沒有「牌桌中央留空給遊戲動畫」這一項**。
> 中央動畫區固定留 82px 的話，iPhone SE 橫式（375px）直接塞爆 ——
> 實測 17 張牌互相壓到。
>
> 目前的處置：中央動畫區改成 `clamp(42px, 19vh, 88px)`，
> 桌機／平板給滿、手機縮到剛好夠放一個和牌演出。
> 代價是 SE 橫式的牌河只有 9×12px。
>
> 驗證方式：把四家牌河灌到 18 張（一局的上限），逐張比對 bounding box
> 找跨區重疊。667×375 / 900×440 / 1180×760 三個尺寸目前都是 **0 重疊**。

### 版面空間預算（已試算）

**橫式可行，直式不可行。**

橫向配置：左右兩家各 95px，中央給自己的手牌。

| 機型（橫式） | 可用寬（扣安全區） | 中央寬 | 自己手牌 |
|---|---|---|---|
| iPhone SE | 667 | 517 | 35px |
| iPhone 14/15 | 758 | 568 | 38px |
| 15 Pro Max | 814 | 624 | 43px |

對照兩人版目前是 40px 上限 —— **四人版手牌幾乎不縮水**，因為張數沒變，
只是左右多挖了約 190px 給側家。

縱向（iPhone 14 的 393px）：
HUD 24 + 對家手牌背 18 + 對家副露 24 + 對家牌河 72 + 自己牌河 72
+ 自己副露 26 + 自己手牌 52 + 按鈕列 40 + 間隙 40 = **368px**，剩 25px。緊但排得下。

代價：三家對手的牌都要用小尺寸（對家手牌背 18px、牌河 15–24px、側家旋轉 90 度直排）。
這是雀魂／天鳳手機版的標準作法。

**直式放不下**：393px 寬扣掉左右兩家各 70px，中央只剩 253px，
自己的手牌會變成 16px —— 兩人版實測下限是 26px。四人直式無解。

→ 已在單機版做好的橫式優化（旋轉引導頁、`viewport-fit=cover`、`100dvh`、橫式壓縮）全部沿用。

**7. 四人麻將的規則補完**　✅ 已完成 2026-09-15
- 莊家與連莊
- 東南西北風位（現在字牌只是牌面，不參與計分）
- 四家之間的**點數移轉**（`calcScore` 現在只算玩家一個人的分）
- 流局規則（現在是雙方合計摸牌 `MAX_TOTAL_DRAWS=60`）

> **產出**：`src/scoring.js`（297 行，17 個匯出）
> `test/scoring.test.js` 85 項測試全過，每一項點數移轉都順便驗零和。
>
> | 項目 | 做法 |
> |---|---|
> | 役牌 | 場風／自風／三元牌各一組刻子 +40。莊家的東是連風，算兩次 |
> | 莊家 | 胡牌 ×1.5 |
> | 榮和 | 放銃者付全額 |
> | 自摸 | 非莊自摸莊家付一半、另兩家各四分之一；莊家自摸三家均分 |
> | 流局 | 聽牌者收、未聽者付，總額 80。四家都聽或都沒聽不移轉 |
> | 本場 | 連莊與流局各 +1，每本場讓和牌者多收 20 |
> | 連莊 | `renchan:'none'`（**預設**，固定四局）／`'repeat'`（標準連莊，打到莊家輪滿一圈，有硬上限保護時間預算） |
>
> **⚠️ 點數尺度改成 ×10**（基本和牌 8 → 80，特殊牌型 8～88 → 80～880）。
> 原因：四人局要把點數拆給三家，自摸一次拆成 4/2/2，
> 任何百分比技能套在 2 上取整之後就消失了 —— 齊「善守」×0.7 等於沒有這個技能。
> 常數在 `scoring.js` 的 `POINT_SCALE`。
>
> **齊「善守」已可用** —— 它卡的就是這一項。減免的結果是**贏家少收**而不是
> 別人補差額，所以整場守得住零和（有測試釘死）。
>
> 為什麼不改 `rules-core.js` 的 `calcScore`：那支是產生器產的（文件寫明不要手改），
> 四人化疊在 `scoring.js`，重跑產生器不會把四人邏輯洗掉。
>
> 修掉一個權威打架：`startHand` 原本自己在 `handNo > totalHands` 時判定整場結束，
> 跟連莊的結束條件互相衝突，導致連莊模式打不滿一圈就被腰斬。
> 現在「打完了沒」只有 `scoring.nextDealer` 一個權威，`match.handLimit` 只是防跑飛的欄杆。

### 階段一補遺（PLAN 原本漏掉的）

原本的 PLAN 把「發牌占卜」與「全程鍵盤／手把」都漏了。兩項都不屬於四人化也不屬於線上化，
但都是玩家會直接碰到的東西，補在這裡。

**A. 全程鍵盤／手把操作**　✅ 已完成 2026-09-16
舊版**完全沒有手把支援**（`getGamepads` 零筆），鍵盤也只有牌桌那一段。
現在標題、選國、牌桌、彈窗都能只用方向鍵＋確定／取消走完。

> **產出**：`src/input.js`（171 行）
>
> | | 鍵盤 | 手把 |
> |---|---|---|
> | 移動 | ← → ↑ ↓ | D-pad／左搖桿（按住連發） |
> | 確定 | Enter／空白 | A／Start |
> | 取消 | Backspace | B |
> | 快速棄牌 | `` ` 1 2 3 … = ⌫ `` | — |
>
> 呼叫端只給一個 `getItems()` 回傳「這個畫面現在可以選的東西」，
> 這一層不認識版面也不記 DOM 結構。彈窗開著時 `getItems()` 只回彈窗的按鈕，
> 所以方向鍵不會跑去動手牌（實測過）。
>
> ↑↓ 用**實際座標**找正上／正下最近的元素，不是固定每列幾個 ——
> 選國畫面每列幾張會隨視窗寬度變，寫死會在窄螢幕上跳錯。
>
> **兩個踩過的坑：**
> 1. `paint()` 只 toggle 目前這批元素的焦點框，換畫面後舊畫面的框會留著 ——
>    畫面上同時出現兩個焦點。要先清掉不在目前這批裡的。
> 2. 一開始讓 `onFocus` 去同步 `selected`，結果每次重畫都覆寫掉滑鼠點選的狀態，
>    兩邊互相打架。**焦點與選取是兩件事**：
>    鍵盤有焦點框當指示，Enter 直接打出（跟數字鍵一致）；
>    滑鼠沒有游標以外的指示，維持點兩下。
>
> ⚠️ 不要把任何功能綁在 Esc 上 —— 瀏覽器保留給「離開全螢幕」，`preventDefault()` 擋不住。

**B. 發牌占卜與下注**　⬜ 未做
舊版 `showDealAnalysis` + `analyzeHand`：開局看自己 13 張 → 文字分析
（花色分布、對子刻子數、可能的特殊牌型提示）→ 下注三選一：

| 下注 | 倍率 | 舊版說法 |
|---|---|---|
| 普通胡牌 | ×1 | 穩扎穩打 |
| 押特殊牌型 | ×2 | |
| 孤注一擲 | ×3 | |

`rules-core.js` 的 `calcScore(special, bet, kingdomId)` **已經吃 bet 參數**，
目前一律傳 `'normal'`。所以計分那端是現成的，缺的是 UI 與三個四人版才有的決策：

1. **四家都下注嗎？** AI 怎麼決定？
2. **下注影響誰？** 只影響自己的得分，還是放銃者也要付 3 倍？
   （影響放銃者的話，別人的下注會左右你的損失，那需要讓所有人看得到彼此的注）
3. **`analyzeHand` 的特殊牌型提示要不要照搬？** 它會洩漏「這手有機會做清一色」，
   四人局裡這是自己的資訊，不影響公平，但那段偵測程式約 80 行、與 `SPECIAL_HANDS` 重複。
   可以改成直接跑 `checkSpecialHand` 的近似判定，少維護一份。

> 註：**特殊牌型本身已經完成**（10 種在 `rules-core.js`，`scoring.js` 會判、結算會顯示、
> 音效分一般與特殊）。占卜缺的只有「開局分析＋下注」這一層。

### 階段二：征服系統

8. 開局選征服目標的 UI　✅ 已完成 2026-09-16（做在選國畫面的第②步，見第 6 項）
9. 技能成長參數化 —— 把寫死的常數改成 `conquered.length` 的函數
   - ✅ **七國技能已重新設計並定案 2026-09-15**，全文見 `docs/skill-conflicts.md` 第六節。
     原則是「技能只作用在自己身上」—— 舊版七個裡只有楚、秦符合，其餘四個
     分別動到共用牌山、共用流局線、或別人的私有手牌，在四人／線上版都站不住。

   | 國 | 新效果 | 成長（依 `conquered.length`，門檻 0–1 / 2–3 / 4+） |
   |---|---|---|
   | 齊 善守 | 對手胡牌時本家應付點數 ×0.7 | 0.7 → 0.6 → 0.5 |
   | 楚 雄兵 | 胡牌得分 ×1.3（不變） | 1.3 → 1.4 → 1.5 |
   | 燕 奇謀 | 看**自己**接下來要摸的 3 張 | 1 → 2 → 3 次/局 |
   | 韓 精兵 | 與牌山最末張交換 1 張手牌 | 1 → 2 → 3 張/局 |
   | 魏 鐵騎 | 下張棄牌不被吃碰槓，改**一場一次** | 1 → 2 → 3 次/場 |
   | 趙 騎射 | 同韓，換 2 張 | 2 → 3 → 4 張/局 |
   | 秦 虎狼 | 特殊牌型得分再 ×1.5（不變） | 1.5 → 1.6 → 1.7 |

   - ⚠️ 齊「善守」**依賴階段一第 7 項**（四家點數移轉）—— 現在的 `calcScore`
     只算一個人的分，「應付的點數」這個東西還不存在。
   - 韓／趙的換牌原語 `swapWithWallTail()` 已經在 `src/game-state.js` 實作並測過。
10. 一場四局的結算與土地增減
11. Game over 後重選國家的流程

> 做完階段一、二，就有完整可玩的四人單機麻將。

### 進度總表（2026-09-22）

| 階段一 | 狀態 | 產出 | 測試 |
|---|---|---|---|
| 1 抽出純規則模組 | ✅ | `rules-core.js` 468 | 37 |
| 2 狀態重構 seats[4] | ✅ | `game-state.js` 435 | 112 |
| 3 宣告仲裁 | ✅ | `claim-arbiter.js` 410 | 98 |
| 4 流程狀態機 | ✅ | `flow.js` 577 | 105 |
| 5 AI 改造 | ✅ | `ai.js` 376 | 71 |
| 6 四方版面 | ✅ | `render.js` 513／`tiles.js` 325／`layout.js` 177 | 18 |
| 7 四人規則補完 | ✅ | `scoring.js` 297 | 85 |
| A 全程鍵盤／手把 | ✅ | `input.js` 182 | 畫面確認 |

| 階段二：征服系統 | 狀態 | 產出 | 測試 |
|---|---|---|---|
| 8 開局選征服目標 | ✅ | 選國畫面第②步 | — |
| 9 技能成長依征服數 | ✅ | `campaign.js` 的 `growthTier` | 332 |
| 10 一場四局結算＋土地增減 | ✅ | `campaign.js` + `mj4.html:settleMatch` | ↑ |
| 11 重整旗鼓（重選國家） | ✅ | `campaign.js:repick` + `mj4.html:1326` | ↑ |
| 存檔（localStorage） | ✅ | `mj4.html:loadCampaign/saveCampaign` | — |

**階段一、二全部完成。四人單機麻將已經可以完整玩一場。**

| 順手完成的別階段工作 | 原屬 |
|---|---|
| 12 MB 拆圖 → `assets/` 157 檔 9.7 MB | 階段四第 20 項 |
| 音效與 BGM 移植 → `audio.js` 1038 行 | — |
| 七國資料 → `kingdoms.js`；君主頭像 `assets/avatar/` | — |
| 本機啟動台與開發工具框架 | — |

### PLAN 沒寫、但做出來的東西

這些是做的過程中長出來的，原本的 21 項清單裡沒有：

| 模組 | 行數 | 測試 | 做什麼 |
|---|---|---|---|
| `fortune.js` | 353 | 60 | 發牌占卜（起手分析與下注）。舊版 `analyzeHand` 沒照搬，重寫的，理由見 `docs/fortune-analysis.md` |
| `deal-seed.js` | 313 | 67 | 偏置發牌（讓起手偏向某個特殊牌型），規格見 `docs/biased-deal.md` |
| `campaign.js` | 228 | 332 | 一場四局之上的戰役層：征服、成長、追趕、重整旗鼓 |
| `decree.js` | 159 | 70 | 出師奏章（開打前的竹簡，四篇依連敗與征服數挑） |
| `layout.js` | 177 | — | 牌桌版面所有可調數值，搭配「對戰畫面編輯器」即時調 |
| `devtools/` 框架 | — | — | 啟動台＋工具自我描述＋版號摺疊 |

### 目前規模（2026-09-22）

- `src/` 17 個模組，**6,014 行**，全部零 DOM 依賴、可在 node 下測
- `test/` **1,055 項**，`./tools/run-tests.sh` 約 45 秒（其中大半是 AI 測試）
- `mj4.html` 2,357 行（含介面與畫面串接）
- `assets/` 157 檔 9.7 MB


### 發牌動畫（2026-09-23 決定）

順序從 `竹簡 → 占卜（蓋住）` 改成 **`竹簡 → 發牌動畫 → 占卜`**。

原因是實測發現：牌其實**已經發好也畫好了**（量到 583 次 DOM 異動），
占卜視窗才蓋上去（`rgba(4,10,7,.93)` 幾乎不透明）。
玩家從頭到尾沒看過自己的牌長什麼樣就要下注。

動畫三段：四家的牌**背面**飛進牌桌 → 自己那 13 張翻開（仍是亂的）→ 自動排序。

**實作方式**：獨立的 `src/deal-anim.js`，跟 `revealFoes()` 同一個模式 ——
自己建一層動畫容器，跑完移除再交回正常渲染。
⚠️ **不動 `render.js` 的渲染模型**（它每次 `clear()` 後重建所有牌，
CSS transition 沒辦法跨越重繪）。

⚠️ **座標一律從實際畫面量**（`getBoundingClientRect`），不要自己算一份版面。
理由同對戰畫面編輯器那條：抄一份過來自己畫，結構就會跟遊戲對不上。

**四個前提（缺一不可）**：

1. **可跳過** —— 任意鍵或點一下就跳到結束狀態
2. ~~第二局起縮短~~ —— **做了之後拿掉**（2026-09-23）。
   原本擔心一場跑四遍會膩，所以第二局起整體加速 0.55 倍；
   實際玩起來**節奏反而變得不一致**，四局都用同一個速度比較好。
   要快就按鍵跳過（第 1 項）
3. **`#preview` 完全跳過** —— 對戰畫面編輯器靠 `if (betPending) chooseBet('normal')`
   關掉占卜，加了動畫之後它會量到**動畫中途的座標**。
   CLAUDE.md 那句「編輯器騙人比沒有編輯器更糟」講的就是這個
4. **尊重 `prefers-reduced-motion`** —— 選國畫面已經有這個處理，比照辦理

**凍結**：不需要另外發明機制。`betPending` 已經是現成的凍結旗標
（五處守衛：`apply()` 不排代打、AI 不動、彈窗藏起來），
改法只是把**旗標與視窗拆開** —— 旗標照樣立刻設，動畫跑完才叫 `askFortune()`。

**線上版**：動畫是純本機表現，四台各播各的、不需要同步。
但四個人會在略微不同的時間播完，階段三第 16 項接牌局同步時要注意
別讓動畫擋住收狀態。

### 階段三：線上化

12. ✅ **開一個新的 Firebase 專案**（2026-09-22，`jcmj-4p`，新加坡） —— 不能跟電鐵共用，`watchGroups` 是對整個 `/groups` 做 `on('value')`，房間會混在同一個大廳
13. ✅ 移植 `net-v1.22.js` → `src/net.js`（996→949 行） —— 砍掉電鐵專屬的 `voteAnnual`/`watchAnnual`（年度結算）與整套 `offers`（存檔比對），改 `GROUP_NODES` 名單
14. ✅ 大廳 UI 移植 → `src/lobby.js`（259 行）＋ `mj4.html` 的 HTML/CSS，
    交棒介面是 `MJLobby.init({onEnterGame(ctx)})`（現在糾纏在 `main-v1.81.js` 的 950 行裡）
15. ✅ 匿名登入 + 權限規則（`hands/{群}/{uid}` 只有本人可讀，已實測別人讀會被拒）
16. 牌局同步 —— 沿用雙通道（輕量 `/live` + 完整 `/states`），payload 重寫
17. 宣告視窗上網 —— 用電鐵年度結算那套「各自寫一格、全員到齊、逾時自動」的成熟模式
18. 斷線重連 —— 沿用電鐵的 `away` 標記機制
19. 房間屬性：AI 難度、房內玩家征服數，顯示在大廳列表

### 本機開發環境（2026-09-16）

**遊戲不需要伺服器 —— 雙擊 `mj4.html` 就能玩。**

`src/*.js` 全是傳統 script（不是 ES module）、圖走 `<img>`，`file://` 底下都載得動。
唯一會擋的是 `fetch()`（CORS），所以資產清單做成 `assets/manifest.js`（`<script>` 載）
而不是 `manifest.json`（`fetch` 讀）—— 兩份同源，由 extract-assets.py 一起產生。
⚠️ **不要把它改回 `fetch()`**，那會讓整個專案變成非要伺服器不可。

無頭 Chrome 實測 `file://`：手牌 14 張 canvas、四個名牌頭貼、中央主公、狀態列全部正常。

**Firebase 跟本機伺服器是兩回事。** Firebase 是階段三的事，
用途是四台瀏覽器之間同步牌局狀態；本機伺服器只是把檔案送進瀏覽器。


雙擊 `啟動戰國麻將.command` → 起伺服器 → 開 `啟動台.html`。
形狀仿小球貓電鐵的啟動台：伺服器狀態列、掃硬碟來的清單、一頁入口。

| | |
|---|---|
| `啟動戰國麻將.command` | Finder 雙擊就跑，等伺服器真的起來才開瀏覽器 |
| `啟動台.html` | 主程式／測試／文件／模組／工具／資產，清單走 `/api/index` |
| `tools/dev-server.py` | no-store 靜態伺服器 + `/api/index`、`/api/tests` |
| `devtools/` | 開發小工具，丟進去就會自己出現在啟動台 |

**啟動台的主要用途是放開發小工具**（仿電鐵的地圖編輯器、卡片編輯器那些）。
掃描規則跟電鐵一樣（家族＋版號，舊版收摺疊區），但有一個改進：
**工具的名字／說明／圖示／分類從工具檔自己的 `<title>` 與 `<meta name="launcher-*">` 讀**，
不像電鐵寫死在啟動台的 META 表裡 —— 新增工具不用回頭改啟動台。

目前有：`devtools/牌面檢視器v0.1.html`（美術）——
一次看完 34 張牌面，可調尺寸／旋轉／薄版／牌背，
並排比對**原版美術與程序繪製**，做美編時直接對照。

伺服器只做兩件事：讓「跑一次測試」按鈕能呼叫 `/api/tests`（跑 `tools/run-tests.sh`，
結果直接顯示在頁面上，實測 26 秒、503 項），以及送 no-store 讓開發時不會載到快取的舊模組。

⚠️ **本專案只在本機跑，這個資料夾刻意沒有 git repo。**
線上版 `allenkwl.github.io/jcmj/` 是兩人單機版且已經有玩家在玩 ——
啟動台上用黃框常駐提醒這件事，免得日後忘記。

### 階段四：上線品質

20. **12MB 單檔拆分**，圖片外部化 —— 四個人都要載完才能開局，行動網路上會很痛
    - 🔶 **拆圖已經在第 6 步順手做掉了**（美術移植本來就要拆）。
      `tools/extract-assets.py` → `assets/` 65 個檔、8.56 MB。
    - 實測發現一件跟原本假設不同的事：**牌面圖只佔約 200 KB，
      11 MB 全是背景圖**（標題／選國／七國君主／勝利／敗亡）。
      所以「四個人都要載完才能開局」其實只需要那 200 KB，
      背景延後載就好 —— 這一項比原本估的輕很多。
    - 另外舊版所有圖都宣告成 `image/png`，實際位元組是 **JPEG**
      （瀏覽器自己嗅探所以看不出來）。抽取工具依實際位元組決定副檔名。
21. 部署（GitHub Pages + 新 Firebase 專案）

---

## 四、技術要點備忘

### 現有程式碼位置（單機版 `戰國麻將列傳.html`）
| 區段 | 行號 |
|---|---|
| CSS | 16–826 |
| DOM | 828–1233 |
| 主邏輯 script | 1234–7363 |
| 規則核心純函式 | 3714–3849、4745–4818、4006 |
| AI | 4615–4907 |
| 中斷處理 | 4910–5060 |
| 戰國主題 | 2378–2470 |
| 存檔 | 3032–3130 |

檔案 7,803 行 / 12.3 MB，其中 **65 行是 base64，佔 11.97 MB（97%）**。
純 JS 約 6,254 行，真正的規則+流程約 3,000 行。
**零模組化**：182 個頂層 function 全在全域，唯二封裝是 `SFX` 和 `BGM` 兩個 IIFE。

### 電鐵連線層可重用範圍（`net-v1.22.js`，996 行）
**約 80% 零遊戲依賴，可直接搬**：
Firebase init / clientId / keyOf / 群組 CRUD / ping-pong 存活探測 / `openOrReclaim` /
onDisconnect 在線判定 / `watchRoom` / claims 座位認領 / ready 門檻 /
**token 回合權威（含 turn 序號、ack）** / beat 心跳 / cmds 演出指令 / pause 斷線暫停 /
`maybeReassignHost` / `purgeGroup` / nudges

**最值得保留的三個設計**（踩過坑換來的，重寫容易再踩）：
1. token 的 `turn` 序號單調遞增防封包亂序
2. claims 的 `away` 標記而非刪除（刪掉就再也回不來）
3. live 每幀重述現況，而非送開關指令（漏一幀自動修正，指令漏一則永遠卡住）

### 宣告視窗的現成樣板
電鐵的年度結算 `/annual/{群}/{階段}/{座位}` 就是
「每台寫自己那一格、全員到齊才推進、兩輪各 10 秒逾時自動按」，
形狀與麻將的吃碰槓胡宣告完全吻合，而且是實戰驗證過的。

### 延遲預算（實測推算）
Firebase `asia-southeast1`（新加坡）來回約 120ms。一次宣告視窗：
打牌寫入 → 三家收到 → 玩家思考 → 三家寫回 → driver 裁決 → 推送結果
≈ **2.1 秒**（含思考）／全 pass 最快 **0.6 秒**。
對照實體麻將每手 2–4 秒，可接受。

### Firebase 免費額度（Spark）
四人麻將完整狀態 ≈ **1.4 KB**（電鐵是 10KB）。
- 10 GB 月流量 → 可跑約 **22,000 局**
- 同時連線 100 → 可開 **25 桌**

綽綽有餘。

---

## 五、風險

| 風險 | 嚴重度 | 對策 |
|---|---|---|
| 手牌隱藏 | 高 | 匿名登入 + 每座位私有節點（已定案） |
| 宣告視窗塞車 | 中 | 無牌可宣告者不跳窗；逾時自動 pass |
| 12MB 首次載入 | 中 | 階段四拆圖 + SW 快取 |
| 狀態重構工程量 | 高 | 先抽純規則模組，降低改動面 |
| 發牌那台仍全知 | 低 | 朋友局可接受；若要根治需 Cloud Functions（Blaze 方案） |
| 燕「奇謀」與手牌隱藏互斥 | 中 | 技能改成公開資訊的強化；見 `docs/skill-conflicts.md` |
| 韓／趙換牌動到共用牌山 | 中 | 換掉的牌放回牌山尾端、從王牌側取，總數與序列都不動 |

### 最大的三塊工作
**狀態重構、宣告仲裁、流程狀態機** —— 都在階段一。這三個做完，後面就順了。
